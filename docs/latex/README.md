# Kernel mathematics

`kernel_mathematics.tex` is the canonical mathematical reference for the
Triton kernels under `gpurec/core/kernels`.

Build it with:

```sh
latexmk -pdf kernel_mathematics.tex
```

Generated LaTeX artifacts and PDFs are not committed.
