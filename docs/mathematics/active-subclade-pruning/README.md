# Active-subclade pruning

`main.tex` documents active-row subclade pruning in the gpurec adjoint.

Build from this directory with:

```sh
latexmk -pdf main.tex
```

The resulting `main.pdf` and LaTeX intermediates are intentionally ignored.
